/**
 * ChatGPT模型类型定义
 */
// eslint-disable-next-line @typescript-eslint/no-namespace
declare namespace ChatGPTModel {
    type ModelItem = {
        id?: string;
        name?: string;
        cover?: string;
    }
}